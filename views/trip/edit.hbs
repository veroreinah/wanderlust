<div class="container">
  <div class="row">
    <div class="col">
      <a class="btn-back" href="/trips/{{trip._id}}">
        <i class="fas fa-angle-left"></i> Back to trip page
      </a>
      <h1>Edit trip 🏔</h1>
      <hr>
    </div>
  </div>
  <div class="row">
    <div class="col">
      {{#if message}}
        <div class="alert alert-danger" role="alert">
          {{ message }}
        </div>
      {{/if}}
      {{#if successMessage}}
        <div class="alert alert-success" role="alert">
          {{ successMessage }}
        </div>
      {{/if}}
      <form id="createTripForm" action="/trips/{{trip._id}}/edit" method="post" enctype="multipart/form-data">
        <div class="form-group">
          <label for="name">Name</label>
          <input type="text" name="name" id="name" class="form-control" value="{{trip.name}}" required>
        </div>
        <div class="form-group">
          <label for="description">Description</label>
          <textarea name="description" id="description" class="form-control">{{trip.description}}</textarea>
        </div>
        <div class="form-group">
          <label for="dates">Dates</label>
          <input type="text" name="dates" id="dates" class="form-control" value="{{trip.dateRange}}" required autocomplete="off">
        </div>
        <div class="form-group">
          <label for="destination">Destination</label>
          <select name="destination" id="destination" class="form-control" multiple="multiple" required>
            {{#each destinations}}
            <option value="{{placeId}}-{{name}}" selected>{{name}}</option>
            {{/each}}
          </select>
        </div>
        <div class="form-group">
          <label for="picture">Picture</label>
          <input type="file" class="form-control-file" name="picture" id="picture">
        </div>
        <button class="btn btn-wanderlust" type="submit">
          <span>Update</span>
        </button>
      </form>
    </div>
  </div>
</div>